anthropic==0.50.0
langgraph==0.3.5
openai>=1.0
python-dotenv==1.2.2
PyYAML==6.0.1

datasets==3.0.1
numpy>=1.26.4
scipy>=1.14.1
protobuf>=4.25.9
sentencepiece==0.2.0
accelerate>=0.33.0

vllm
transformers>=4.57.0,<5

# Analysis layer (analysis/*.py). Not needed to run experiments.
pandas>=2.0
matplotlib>=3.7

# Local Hugging Face backend (provider: hf), e.g. qwen2.5-0.5b-hf on Apple
# Silicon. torch's macOS arm64 wheel carries the MPS backend; accelerate is
# used for device placement on non-MPS devices. Not needed for API or vLLM runs.
#   pip install torch transformers accelerate
torch>=2.2

# Apple Silicon GPU backend (provider: mlx), e.g. qwen2.5-7b-mlx. macOS
# arm64 only -- these wheels do not exist on Linux/CUDA, so they are not
# installed by default.
#   pip install mlx mlx-lm
